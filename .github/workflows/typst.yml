name: Build Typst document
on: pull_request
jobs:
  build_typst_documents:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Typst
        uses: lvignoli/typst-action@main
        with:
          source_file: "CV.typ"
      - name: Upload PDF file
        uses: actions/upload-artifact@v4
        with:
          name: PDF-${{ env.DATE }}
          path: "**/*.pdf"
      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "cv.pdf"
          body: "PDF generated from cv.pdf"
          commit: "main"
          tag: "latest"
          allowUpdates: true